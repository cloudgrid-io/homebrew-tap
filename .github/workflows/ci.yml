name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  license:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: LICENSE is Apache 2.0
        run: |
          if [ ! -f LICENSE ]; then
            echo "LICENSE file is missing"
            exit 1
          fi
          if ! grep -q "Apache License" LICENSE || ! grep -q "Version 2.0" LICENSE; then
            echo "LICENSE is not Apache License 2.0"
            exit 1
          fi
          echo "LICENSE is Apache 2.0"

  brew-lint:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: brew style
        run: brew style Formula/grid.rb
      - name: brew audit
        run: |
          TAP="$(brew --repository)/Library/Taps/cloudgrid-io/homebrew-tap"
          mkdir -p "$(dirname "$TAP")"
          ln -sfn "$GITHUB_WORKSPACE" "$TAP"
          brew audit --strict --online cloudgrid-io/tap/grid
